<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.animation.mapper.OnlineSiteMapper">
<!--    <select id="GetUserByID" parameterType="int" resultType="com.test.springtest.dao.MUser">-->
<!--        select * from `student` where id = #{id}-->
<!--    </select>-->

    <insert id="save" parameterType="com.project.animation.po.OnlineSite"
            useGeneratedKeys="true">
        insert into online_site(`name`,url,img) values (#{name},#{url},#{img})
    </insert>
</mapper>